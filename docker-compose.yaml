version: "3.3"
services:
  app:
    image: ghcr.io/${{ github.repository_owner }}/${{ github.event.repository.name }}/microservice/${{ vars.MICRO_SERVICE_NAME }}:${{ vars.DOCKER_TAG }}
    ports:
      - 3000:3000
